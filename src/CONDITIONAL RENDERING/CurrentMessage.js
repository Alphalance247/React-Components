import Weekdays from "./Weekdays";
import Weekend from "./Weekend";

// const CurrentMessage = function ({ msg }) {
//   if (msg >= 1 && msg <= 5) return <Weekdays />;

//   return <Weekend />;
// };

// export default CurrentMessage;

// const CurrentMessage = function ({ msg }) {
//   const weekday = msg >= 1 && msg <= 5;
//   const weekend = msg <= 6 && msg <= 7;
//   let message;

//   if (weekday) message = <Weekdays />;
//   if (weekend) message = <Weekend />;
// else  message = console.log("no data");

//   return <div>{message}</div>;
// };

// export default CurrentMessage;

// export default CurrentMessage;
